import 'package:flutter/material.dart';

// Dark Mode
const primaryTextColor = Color(0xFFFFFFFF);
const secondaryTextColor = Colors.white;
const primaryColor = Color(0xFF1A1A2B);
const primaryAccent = Color(0xFFFF004E);
const secondaryAccent = Color(0xFF16C9FF);
const terciaryAccent = Color(0xFFFED813);

const secondaryColor = Color(0xFF5A5875);

const rookieColorDark = Color(0xFF3614FF);
const rookieColorLight = Color(0xFFDED9FF);

const intermediateColorDark = Color(0xFF2DA83E);
const intermediateColorLight = Color(0xFFD9FFDE);

const seniorColorDark = Color(0xFFB39100);
const seniorColorLight = Color(0xFFFFF6CC);

const professionalColorDark = primaryAccent;
const professionalColorLight = Color(0xFFFFD1DF);

const LinearGradient redGradient = LinearGradient(
    colors: [primaryAccent, Color(0xFFFF2222)],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);
const LinearGradient greenGradient = LinearGradient(
    colors: [secondaryAccent, Color(0xFF56FE99)],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);
const LinearGradient goldGradient = LinearGradient(
    colors: [terciaryAccent, Color(0xFFE8CA31)],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);

const LinearGradient rookieGradient = LinearGradient(
    colors: [rookieColorDark, rookieColorLight],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);
const LinearGradient intermediateGradient = LinearGradient(
    colors: [intermediateColorDark, intermediateColorLight],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);
const LinearGradient seniorGradient = LinearGradient(
    colors: [seniorColorDark, seniorColorLight],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);
const LinearGradient professionalGradient = LinearGradient(
    colors: [professionalColorDark, professionalColorLight],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);





// Light Mode
const primaryTextColorLM = Color(0xFF020202);
const secondaryTextColorLM = Colors.black45;
const primaryColorLM = Color(0xFFF3F3F3);
const primaryAccentLM = Color(0xFFFF004E);
const secondaryAccentLM = Color(0xFF16C9FF);
const terciaryAccentLM = Color(0xFFFED813);

const secondaryColorLM = Color(0xFF5A5875);

const rookieColorDarkLM = Color(0xFF3614FF);
const rookieColorLightLM = Color(0xFFDED9FF);

const intermediateColorDarkLM = Color(0xFF2DA83E);
const intermediateColorLightLM = Color(0xFFD9FFDE);

const seniorColorDarkLM = Color(0xFFB39100);
const seniorColorLightLM = Color(0xFFFFF6CC);

const professionalColorDarkLM = primaryAccent;
const professionalColorLightLM = Color(0xFFFFD1DF);

const LinearGradient redGradientLM = LinearGradient(
    colors: [primaryAccent, Color(0xFFFF2222)],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);
const LinearGradient greenGradientLM = LinearGradient(
    colors: [secondaryAccent, Color(0xFF56FE99)],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);
const LinearGradient goldGradientLM = LinearGradient(
    colors: [terciaryAccent, Color(0xFFE8CA31)],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);

const LinearGradient rookieGradientLM = LinearGradient(
    colors: [rookieColorDark, rookieColorLight],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);
const LinearGradient intermediateGradientLM = LinearGradient(
    colors: [intermediateColorDark, intermediateColorLight],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);
const LinearGradient seniorGradientLM = LinearGradient(
    colors: [seniorColorDark, seniorColorLight],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);
const LinearGradient professionalGradientLM = LinearGradient(
    colors: [professionalColorDark, professionalColorLight],
    begin: FractionalOffset.centerLeft,
    end: FractionalOffset.centerRight);